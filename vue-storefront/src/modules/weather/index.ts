// import { StorefrontModule } from '@vue-storefront/core/lib/modules'
// import { weatherModule } from './store'
//
// export const KEY = 'weather-form'
//
// export const WeatherForm: StorefrontModule = function ({ app, store, router, moduleConfig, appConfig }) {
//   store.registerModule(KEY, weatherModule)
// }

import { StorefrontModule } from '@vue-storefront/core/lib/modules';

export const WeatherForm: StorefrontModule = function ({ app, store, router, moduleConfig, appConfig }) {
// code
}
